---
description: Sokoban Brawl 專案規範與現況（技術棧、進度、部署、安全）
alwaysApply: true
---

# Sokoban Brawl 專案規範

## 專案概述與現況
- **類型**：多人競技倉庫番（倉庫大亂鬥）；目前以單人測試與排行榜為主，多人 Socket.io 已接線尚未實作玩法。
- **架構**：權威伺服器（Node.js）、客戶端 HTML5/JS 原型 → 未來 Godot；排行榜與關卡驗證在伺服器。

## 技術棧（當前實作）
- **伺服器**：Node.js 18+、ESM、Express、Socket.io、sql.js（SQLite 記憶體/檔案）、`server/core/validator.js` 推箱路徑驗證。
- **客戶端**：`client/` 靜態 HTML/CSS/JS，預設連 `localhost:3000`，可改為正式站網址。
- **API**：GET/POST `/api/leaderboard/:levelId`、GET `/health`；關卡定義在 `server/levels.json`。
- **部署**：正式站 https://sokoban.lukwama.com，見 `docs/DEPLOYMENT.md`（Nginx + Let's Encrypt + systemd）。

## 開發原則
- **權威伺服器**：關卡過關驗證、排行榜寫入一律在伺服器；客戶端只發送操作意圖或已完成的 moves 序列。
- **傳輸**：JSON；未來多人可補上 `timestamp`、`action_id`、`checksum`（HMAC）防篡改。
- **模組化**：邏輯與介面分離，便於日後遷移至 Godot GDScript。

## 開源與安全（必守）
- 文件與程式碼**不得**包含：實際主機 IP、密碼、API 金鑰、Token。使用佔位符（如 `YOUR_SERVER_IP`）或環境變數說明。
- 敏感設定使用 `process.env` 或 `.env`（已列入 .gitignore），勿提交至 Git。

## 自動重啟與開發流程
- **即時預覽**：本機兼具正式機身分（`sokoban.lukwama.com`），每次修改 `server/` 或 `client/` 的程式碼後，**必須**自動透過 `sudo systemctl restart sokoban-brawl` 重新啟動服務，讓修改立即生效。
- **Git 版本控制 (GitHub 同步)**：
  - 本機已設定 SSH Key 驗證 (`git@github.com:lukwama/sokoban-brawl.git`)。
  - AI Agent 在完成開發階段或使用者要求時，應協助執行 `git push` 將進度推送至遠端，或 `git pull` 取得最新進度。
  - 在推送前，如果尚未設定 `git config`，請自動帶入通用 AI 參數或提醒使用者設定；如果是新機器，可先跑 `ssh -T git@github.com` 確認連線。
- **檢查與併入主線流程（多地同時開發）**：
  - **en_US**: Multi-site workflow: (1) Before starting work, run `git pull origin main` to sync. (2) After changes, run `git status` to see modified/untracked files. (3) Only `git push` does **not** commit; uncommitted changes are not pushed. To merge to main: `git add <files>`, then `git commit -m "..."`, then `git push origin main`. Agent should remind or perform this full sequence when user intends to "push to main".
  - **zh_TW**：多地開發時：(1) 開始前先 `git pull origin main` 同步。(2) 變更後用 `git status` 檢視已改／未追蹤檔案。(3) 僅執行 `git push` **不會**一併提交；未 commit 的變更不會被推上去。併入 main 完整步驟：`git add` → `git commit -m "說明"` → `git push origin main`。使用者表達要「推上主線」時，Agent 應提醒或代為執行此完整流程。

## 文件與註解：國際社群雙語規範
- 本專案面向國際社群，**說明與備註須同時提供 en_US（英文）與 zh_TW（繁體中文）**，方便全球開發者貢獻。
- **順序**：**en_US 區塊在上方、zh_TW 區塊在下方**。
- 適用於：README、docs、API 說明、重要程式註解、Commit message 說明（可擇一語系簡短描述，但正式文件請雙語）。
